#!/bin/sh
repo_root=$(git rev-parse --show-toplevel 2>/dev/null) || {
  echo "gn: could not determine git repo root" >&2
  exit 1
}
tool="$repo_root/tools/gn"
if [ -x "$tool" ]; then
  exec "$tool" "$@"
fi
exec gn "$@"
